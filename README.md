# Python3-installer-Centos
This script installs defined (latest) version of python in centos7-8 system.

This must be ran as a root user. 

to install this on any centos 7/8 do:
sudo ./install_python.sh 3.11.0
